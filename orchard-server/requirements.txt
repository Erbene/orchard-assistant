fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.32
python-dotenv>=1.0        # load orchard-server/.env on bare-metal runs (app/config.py)
httpx==0.28.1
structlog>=24,<27
mcp>=1.29,<2

# PostgreSQL (docker-compose data layer — app/core/db.py)
sqlalchemy[asyncio]>=2.0,<3
asyncpg>=0.30

# RAG knowledge base
chromadb>=1.5,<2
pypdf>=5

# Agent orchestration (app/agent) + Phase 4 Foreman
langgraph>=1.2,<2
langchain-core>=1.0,<2
langchain-mcp-adapters>=0.3,<1
langchain-ollama>=1.0,<2          # local LLM (Foreman narration) - optional at runtime
langsmith>=0.1,<1                 # tracing (app/core/tracing.py); opt-in via LANGCHAIN_TRACING_V2
langgraph-checkpoint-postgres>=3,<4
psycopg[binary,pool]>=3.2

pytest==8.3.4
respx>=0.21,<0.23        # httpx-native mocking for the Rachio service tests
